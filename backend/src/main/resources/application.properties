spring.application.name=apex-test-suite
server.port=8080

# H2 for local development
spring.datasource.url=jdbc:h2:file:./data/apexdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Cache
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterWrite=10m

# Salesforce OAuth2 (configure these with your Connected App)
salesforce.client-id=${SF_CLIENT_ID:}
salesforce.client-secret=${SF_CLIENT_SECRET:}
salesforce.login-url=${SF_LOGIN_URL:https://login.salesforce.com}
salesforce.redirect-uri=${SF_REDIRECT_URI:http://localhost:8080/api/auth/callback}

# CORS
app.cors.allowed-origins=http://localhost:5173
